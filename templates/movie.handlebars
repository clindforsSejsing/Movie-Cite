<a href="/">Home is where the ❤ is..</a>
<article class="movie">
  <h1>{{ movie.attributes.title }}</h1>
  <div class="intro">
    {{{ markdown movie.attributes.intro }}}
  </div>
  {{#if movie.attributes.image }}
  <img class="movImg" src="{{ movie.attributes.image.url }}">
  {{/if }}
  </div>